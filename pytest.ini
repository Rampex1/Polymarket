[pytest]
testpaths = tests
addopts = -ra -q --tb=short
# web3 / eth-account (pulled in by py_clob_client_v2) are a DeprecationWarning
# fountain — they drown real output otherwise.
filterwarnings =
    ignore::DeprecationWarning
