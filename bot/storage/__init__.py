"""
storage

Every SQLite table and the code that reads it: connections and schema (db),
open positions and the trade log (ledger), training-data rows (signals),
and boot provenance (runs). Re-exports nothing — import the submodule.
"""
