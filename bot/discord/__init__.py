"""
discord

Everything that talks to Discord: webhook alerts (notifier), message
formatting (messages), the per-market thread registry (threads), and the
slash-command bot (discord_bot, which runs as its own process).
"""
