"""
domain

Side-effect-free trading concepts shared across strategies and execution:
what we decided (intents), what we hold (position), what the APIs said
(records), and how an algorithm is configured (params).

Import from the submodules; this package re-exports nothing, so there is
only ever one path to a given name.
"""
