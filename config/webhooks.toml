# Discord webhook registry — reserved for future use.
#
# Webhook URLs now live directly in each [[algorithm]] block in the
# profile TOMLs (config/prod.toml, config/experimental.toml) as the
# `webhook_url` param. Add entries here when a use case arises that
# requires routing outside of a single algorithm's scope.
