# Discord webhook registry — single source of truth for which channel each
# profile notifies. bot/config.py routes by matching PROFILE against each
# block's `profiles` list; the DISCORD_WEBHOOK_URL env var, if set,
# overrides the registry (escape hatch only).
#
# Webhook URLs are write-access secrets (anyone holding one can post to the
# channel). Committed on the explicit call that this repo is PRIVATE —
# if the repo ever goes public, rotate every webhook below first; they
# live in git history forever.

[prod]
url = "https://discord.com/api/webhooks/1513777234547048619/XX_f2STzSQDlGiVxOOksbVPF36TYdjzp2hKLJSmQgvNBGxHSxlc0XHN5UKSoAoNnk__h"
profiles = ["prod"]
notes = "Original webhook — real-money trade alerts. Keep low-noise."

[experimental]
url = "https://discord.com/api/webhooks/1514867567519596594/tPzzQrqH5_0X5oXIV2iQnH5hFiSMe4h0iNyvQslN5xNHhSUn7lfPOB4_KRdLDKJbe99Q"
profiles = ["experimental"]
notes = "Paper-trading noise channel, added 2026-06-11."
