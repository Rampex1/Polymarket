# Experimental profile — paper-mode algorithms being tuned or A/B tested.
# Loaded with PROFILE=experimental. Nothing here can touch real money: this
# profile never sets allow_live, so the loader rejects any mode = "live"
# block outright — the guarantee is in config, not in which .env is loaded.

# Fresh-wallet suspicious-flow detector — paper validation phase. Copies
# large long-odds BUYs from young wallets (the documented insider
# fingerprint). All knobs at schema defaults.
[[algorithm]]
type = "insider_flow"
name = "insider_flow_paper"
mode = "paper"

[algorithm.params]
webhook_url = "https://discord.com/api/webhooks/1514867567519596594/tPzzQrqH5_0X5oXIV2iQnH5hFiSMe4h0iNyvQslN5xNHhSUn7lfPOB4_KRdLDKJbe99Q"
max_total_exposure_usdc = 1000000.0   # no cap for paper
